*baya systems*
- Implement MOESI/MESI snoop-based coherence or a simple directory.
- Integrate with processors (e.g., RISC-V) and memory controllers.

*nvidia*
- raytracing with cuda
- learn gpu arch and cuda

*nutanix, oracle*
- Raft-based Distributed Key-Value Store (in Golang)

*samsung*
- HBM, simulations

*texas instrumentation*
- memory controller

adv. comp arch book
modern c++


Add directory-based coherence, multi-level caches, or integrate with a simple NoC. Add performance/power modeling.

Finish in 7–10 days. Then add: SIMD/vectorization, multi-threading (OpenMP/TBB), GPU offload (CUDA/OpenCL if possible), and detailed optimization write-up.

NoC knownledge of baya


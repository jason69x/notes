use `__slots__ = ['x','y']` inside a python class to save space, it restricts class attributes to only these. else python creates a dictionary to tract attributes (and also those generated at runtime)

